EXE = sc.exe		#指定输出文件
OBJS = sc.obj		#需要的目标文件
ML_FLAG = /c /coff		#编译选项 
LINK_FLAG = /subsystem:windows	#连接选项
$(EXE): $(OBJS)
	Link $(LINK_FLAG) $(OBJS)

.asm.obj:
	ml $(ML_FLAG) $<

clean:
	del *.obj
